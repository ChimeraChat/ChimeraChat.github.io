{
  "name": "my-package",
  "license": "UNLICENSED",
  "scripts": {
      "predeploy": "yarn run build",
      "deploy": "gh-pages -d build -b main",
      "start": "yarn",
      "build": "yarn build",
      "test": "test"
  }
}
