{
  "name": "my-package",
  "license": "UNLICENSED",
  "scripts": {
      "predeploy": "npm install && npm run",
      "deploy": "gh-pages -d build -b main",
      "build": "yarn build",
      "start": "npm install pg",
      "test": "jest"
  }
}
