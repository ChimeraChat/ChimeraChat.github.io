// auth.js
/*
import * as api from './api.js';

export async function handleSignup(username, password, email) {
    try {
        const data = await api.signup(username, password, email);
        return { success: true, data };
    } catch (error) {
        return { success: false, error: error.message || "Error signing up!" };
    }
}

export async function handleLogin(username, password) {
    try {
        const data = await api.login(username, password);
        return { success: true, data };
    } catch (error) {
        return { success: false, error: error.message || "Error login in!" };
    }
}

 */